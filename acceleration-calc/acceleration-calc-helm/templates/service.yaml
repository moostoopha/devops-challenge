apiVersion: v1
kind: Service
metadata:
  name: {{ include "acceleration-calc-helm.fullname" . }}
  labels:
    {{- include "acceleration-calc-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acceleration-calc-helm.selectorLabels" . | nindent 4 }}
